import { Request, Response } from "express";
import asyncHandler from "../../utils/asyncHandler";




export const fetchAddedSnacks = asyncHandler(async (req:Request, res:Response) => {

})


export const addSnack = asyncHandler(async (req:Request, res:Response) => {
    
})

